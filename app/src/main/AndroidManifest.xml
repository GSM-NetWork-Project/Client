<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.client">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowCommentActivity"></activity>
        <activity android:name=".answer.AnswerCommentActivity" />
        <activity android:name=".question.QuestionCommentActivity" />
        <activity android:name=".answer.WriteAnswerActivity" />
        <activity android:name=".question.WriteQuestionActivity" />
        <activity android:name=".question.ShowQuestionActivity" />
        <activity android:name=".start.SplashActivity" />
        <activity
            android:name=".start.SignUpActivity"
            android:theme="@style/Material" />
        <activity
            android:name=".start.LoginActivity"
            android:theme="@style/Material" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>